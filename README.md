# Natural-Language-Processing-NLP-
This repo fine-tunes encoder models (BERT, DistilBERT) using Hugging Face Trainer on custom open datasets. It covers NLP tasks like Named Entity Recognition (NER), binary, and multi-class text classification.
